// A4 Dimensions in millimeters
export const A4_WIDTH_MM = 210;
export const A4_HEIGHT_MM = 297;

// Conversion factor roughly for screen display (CSS pixels per mm is approx 3.78, but we scale visually)
export const MM_TO_PX = 3.78; 

export const DEFAULT_IMAGE_URL = 'https://picsum.photos/800/600';
